import React from 'react'
import Base from '../core/Base'

const PurchaseList = () => {
    return (
        <Base title="Purchases Page">
            <h1>Purchases Page</h1>
        </Base>
    );
};

export default PurchaseList;
